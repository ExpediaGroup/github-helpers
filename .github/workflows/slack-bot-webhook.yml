name: Slack Bot Webhook

on:
  pull_request:
    branches: [main]
    paths:
      - 'src/helpers/slack-bot-webhook.ts'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - uses: ./
        with:
          helper: slack-bot-webhook
